# Harness-generated state
.claude/scheduled_tasks.lock

# Agent working artifacts (plans and investigation notes). Remove these
# entries if this project wants them version-controlled.
plans/
notes/

# Memory contents can hold personal or project-private facts.
# The index stays tracked because CLAUDE.md imports it.
memory/*
!memory/MEMORY.md

# macOS junk
.DS_Store
